<UserControl x:Class="ToolsManager.Client.Views.ViewTemplateEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:Atlante.Localization;assembly=Atlante.Localization"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             Width="Auto" Height="Auto" MinWidth="450" MinHeight="110">
    <Grid>
        <Label Content="{loc:Translate Key=UI_DESCRIPTION}" Height="28" HorizontalAlignment="Left" Margin="6,6,0,0" VerticalAlignment="Top" Width="128" HorizontalContentAlignment="Right" />
        <TextBox Text="{Binding Description}" Margin="144,6,6,0" VerticalAlignment="Top" />
        <Label Content="{loc:Translate Key=UI_CONFIGURATION_FILE}" Height="28" HorizontalAlignment="Left" Margin="6,42,0,0" VerticalAlignment="Top" Width="128" HorizontalContentAlignment="Right" />
        <TextBox Text="{Binding ConfigFile}" Margin="144,45,6,0" VerticalAlignment="Top" />
        <Label Content="{loc:Translate Key=UI_TYPE}" Height="28" HorizontalAlignment="Left" Margin="6,78,0,0" VerticalAlignment="Top" Width="128" HorizontalContentAlignment="Right" />
        <TextBox Text="{Binding Type}" Margin="144,81,6,0" VerticalAlignment="Top" />
    </Grid>
</UserControl>
